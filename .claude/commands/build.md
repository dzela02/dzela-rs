---
description: Build the SSR production bundle
---

Run `pnpm build` from the monorepo root. The output goes to `apps/web/.output/`. Report build time, bundle size if available, and any warnings or errors. If the build fails, show the relevant error lines and suggest a fix.
