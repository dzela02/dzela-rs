---
description: Run TypeScript type checking across all workspaces
---

Run `pnpm typecheck` from the monorepo root. Report every type error with file path and line number. If there are no errors, confirm clean. Do not attempt to fix errors unless the user asks.
