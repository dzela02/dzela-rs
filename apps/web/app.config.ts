// This file is intentionally empty.
//
// Older TanStack Start releases (Vinxi-based) required `app.config.ts`. The
// modern Start (>=1.120) uses a plain `vite.config.ts` instead — see
// ./vite.config.ts. This file is kept as a stub so legacy imports/tools that
// look for it don't error.
export {};
