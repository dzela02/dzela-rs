// Modern TanStack Start (>=1.120) auto-generates the client entry through the
// `tanstackStart` Vite plugin — there is no hand-written `client.tsx` anymore.
// Kept as a stub for legacy tooling that may still look for the file.
export {};
