// Tailwind is intentionally NOT used in this project — the design system is
// hand-rolled CSS (see src/styles/tokens.css and src/styles/app.css).
// This file is kept as a stub so accidental imports fail loudly rather than
// silently picking up arbitrary defaults.
export default {};
